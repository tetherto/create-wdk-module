{
  "name": "{{PACKAGE_NAME}}",
  "version": "1.0.0-beta.1",
  "description": "{{DESCRIPTION}}",
  "keywords": ["wdk", "fiat", "on-ramp", "off-ramp"],
  "author": "{{AUTHOR}}",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git://github.com/YOUR_ORG/{{MODULE_NAME}}.git"
  },
  "main": "index.js",
  "type": "module",
  "types": "./types",
  "scripts": {
    "build:types": "tsc",
    "lint": "standard --env jest",
    "lint:fix": "standard --fix --env jest",
    "test": "cross-env NODE_OPTIONS=--experimental-vm-modules jest",
    "test:coverage": "cross-env NODE_OPTIONS=--experimental-vm-modules jest --coverage"
  },
  "dependencies": {
    "@tetherto/wdk-wallet": "^1.0.0-beta.1"
  },
  "devDependencies": {
    "cross-env": "7.0.3",
    "jest": "^29.7.0",
    "standard": "17.1.2",
    "typescript": "5.8.3"
  },
  "exports": {
    ".": {
      "types": "./types/index.d.ts",
      "bare": "./bare.js",
      "default": "./index.js"
    },
    "./package": {
      "default": "./package.json"
    }
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "standard": {
    "ignore": [
      "bare.js",
      "tests/**/*.js"
    ]
  }
}
